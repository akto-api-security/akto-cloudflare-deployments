{
  "name": "akto-cloudflare-proxy",
  "version": "0.0.1",
  "description": "Cloudflare Worker that transparently proxies traffic, sends it to Akto for API discovery, and optionally enforces MCP guardrails",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.46.0"
  }
}
